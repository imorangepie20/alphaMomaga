# Signed JWT verification configuration
AUTH_JWKS_URL=https://identity-provider.example/.well-known/jwks.json
AUTH_ISSUER=https://identity-provider.example/
AUTH_AUDIENCE=property-management-api

# Local-only compatibility fallback; keep false in shared environments
AUTH_ALLOW_DEMO_ROLE=false
